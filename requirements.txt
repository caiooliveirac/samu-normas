Django==5.2.6
python-dotenv>=1.0
gunicorn>=21.2
django-extensions>=3.2
mysqlclient>=2.2
Pillow>=10.0
# opcional: descomente se quiser usar rate limit depois
# django-ratelimit>=4.1

# Dependências de desenvolvimento (pytest, etc.) ficam em requirements-dev.txt
